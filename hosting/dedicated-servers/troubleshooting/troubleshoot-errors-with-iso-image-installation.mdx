---
title: Troubleshoot errors with ISO image installation
sidebarTitle: ISO images
ai-navigation: Troubleshoot ISO image upload and mounting errors in Dedicated Servers - check lowercase .iso extension, 10 GB size limit, and five-image limit per location.
---

ISO images for dedicated servers are managed in DCI manager under **Templates** > **ISO-images**. The [ISO&nbsp;installation](/hosting/dedicated-servers/manage/operating-system/install-a-linux-os-from-your-iso-image) article covers the full upload and boot process.

## Upload failure

If the ISO image upload fails, check that the file meets all requirements:

- The file extension must be lowercase: `.iso`, not `.ISO`. Use only letters and numbers in the filename.
- The file size must not exceed 10 GB per location.
- No more than five ISO images are allowed per location. ISO images are automatically deleted after 24 hours.

If the upload still fails, delete the failed image and try again. DCI manager shows a **Delete the ISO-image** button in the error dialog.

## Mounting error

If DCI manager shows the following error when mounting an ISO image on a server:

```
Error occurred when mounting ISO-image on the IPMI proxy server. Script output: ''
```

Delete the ISO image and upload it again. If the error persists, [contact&nbsp;support](/hosting/contact-our-technical-support).
