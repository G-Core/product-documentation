---
title: Create a CDN resource for video streaming
sidebarTitle: For video streaming
ai-navigation: Create a CDN resource for video streaming with HLS, DASH, or FLV, configuring caching, geo-restrictions, secure tokens, and Origin Shielding.
---

Gcore Video CDN delivers live streams and video-on-demand content globally without transcoding, using existing HLS, DASH, or FLV output.

## Video CDN or Gcore Video Streaming

Choose Video CDN when prepared streams already exist, or Gcore Video Streaming when transcoding and a hosted workflow are required.

<Tabs>
  <Tab title="Use Video CDN if...">
    Use [Video&nbsp;CDN](/cdn/video-cdn/video-cdn-overview) when:

    - Existing HLS, DASH, or FLV streams are already available
    - Transcoding is handled independently, outside Gcore
    - Global delivery at scale is required (thousands to millions of viewers)
    - Advanced CDN features are needed:
      - Geo-blocking by country
      - Secure token authentication
      - Custom domains
      - Origin Shielding
    - Reducing origin bandwidth costs is a priority

    Supported formats:

    - [HLS](/cdn/video-cdn/hls) (`.m3u8` playlists, `.ts` or `.m4s` segments)
    - MPEG-DASH (`.mpd` manifests)
    - HTTP-FLV (`.flv` streams)
    - MP4 (progressive download, VOD only)

    Example scenarios:

    - A media server (Wowza, Nginx-RTMP, SRS) produces HLS output
    - Migrating from another CDN (Cloudflare, AWS, or Akamai)
    - Broadcasting rights require geo-restrictions
    - Offloading bandwidth from the origin is a goal
  </Tab>
  <Tab title="Use Video Streaming if...">
    Use Gcore Video Streaming instead when:

    - Transcoding is needed for raw RTMP/SRT input
    - Automatic quality ladder generation (ABR) is required
    - Streaming originates from encoders (OBS, vMix, hardware encoders)
    - DVR (pause/rewind) or recording features are needed
    - An all-in-one hosted solution is preferred

    Gcore Video Streaming setup starts from the [Video&nbsp;Streaming](/streaming) section.
  </Tab>
</Tabs>

---

## Quick setup

Create a Video CDN resource in five steps: request activation, choose a paid plan, create the resource, set caching rules, and enable the LIVE STREAMING preset for live delivery.

<Steps>
  <Step title="Request streaming activation">
    <Warning>
    Video delivery via CDN requires legal approval from Gcore Support before setup can continue.
    </Warning>

    Contact [Gcore&nbsp;Support](mailto:support@gcore.com) or use the chat in the bottom-right corner and ask to enable video delivery for the CDN account. This is CDN-side approval for prepared HLS and DASH delivery.

    Provide:

    - Account ID (found in the profile dropdown or [Account&nbsp;overview](/account-settings/account/overview))
    - Domain name and company name
    - Content type: sports, education, entertainment, or news
    - Sample stream URL: an example of the typical stream format
    - Viewer geography: where the audience is located
    - Broadcasting license or content compliance policy
    - Abuse or copyright contact email

    For sports, TV channels, events, or other third-party protected content, Support may request proof that the account holder or its clients have rights to distribute the content. Requests without the necessary licensing details, company identity, or abuse handling information may be declined or closed.

    Processing time is usually 1–2 business days.

    <Frame>![Account ID location in the profile dropdown](/images/docs/cdn/getting-started/create-a-cdn-resource/create-a-cdn-resource-for-video-streaming/create-a-cdn-resource-for-video-streaming-image1.png)</Frame>
  </Step>

  <Step title="Activate a paid CDN plan">
    Video streaming requires a paid CDN subscription.

    1. Navigate to **CDN** in the [Gcore&nbsp;Customer&nbsp;Portal](https://portal.gcore.com).

    2. On the free plan, upgrade to:
       - **Start** (1.5 TB included traffic)
       - **Pro** (5 TB included traffic)

    Pricing is based on bandwidth delivered to viewers. Cost details are on the [pricing](/cdn/how-the-cdn-service-and-its-additional-options-are-billed) page.
  </Step>

  <Step title="Create a CDN resource">
    Once Support activates streaming, create the CDN resource:

    1. Navigate to **CDN** > [CDN&nbsp;resources](https://portal.gcore.com/cdn/resources/list).

    2. Click **Create CDN resource**.

    3. Enter the domain that will serve the Video CDN — for instance, `video.example.com`. Use **Add CNAME** to add more domains if needed.

    4. Under **Origin**, keep **Specify content origin** selected, then select the origin type:

    <Tabs>
      <Tab title="URL">
        - **Origin source**: the media server domain or IP — for instance, `live.example.com`
        - The origin must be resolvable through DNS — internal-only or placeholder domains are rejected
      </Tab>
      <Tab title="S3 storage">
        - **Origin source**: the Gcore Storage bucket that holds the VOD files
      </Tab>
      <Tab title="FastEdge">
        - **Origin source**: the FastEdge app that generates the response
      </Tab>
    </Tabs>

    5. Click **Create**.

    The [static&nbsp;assets](/cdn/getting-started/create-a-cdn-resource/create-a-cdn-resource) guide covers the same resource creation process used for video.
  </Step>

  <Step title="Configure caching rules">
    Optimize caching for the delivery type.

    <Tabs>
      <Tab title="Live HLS">
        Basic cache configuration for live streams.

        Enable Always online first:

        1. Navigate to **CDN Resource** > **Cache**.

        2. Enable **Always online** and add these tags to the list of error types that trigger it:
           - error (serve stale cache if origin returns errors)
           - timeout (serve stale cache if origin times out)

        Always online protects viewers from origin failures. If the origin goes down, the CDN serves cached content instead of error pages, which improves reliability and uptime.

        <Warning>
        Do not add "updating" to the error types list for live streaming. When excluded, the CDN can deliver video chunks more efficiently and reduce buffering. Only add "error" and "timeout".
        </Warning>

        Set the resource-level cache for segments:

        1. Still in **CDN Resource** > **Cache**.

        2. Set **CDN cache expiry time** to 60 seconds.
           - This caches all content (except manifests, see below) for 60 seconds
           - With DVR enabled, increase this to match the DVR window (30 minutes, for instance)

        Create a manifest caching rule:

        1. Navigate to **CDN Resource** > **Rules**.

        2. Click **Create rule** and select **Playlists template (.m3u8)** to pre-fill the pattern and cache settings for manifest files.

        <Frame>![Create rule dropdown with the Playlists and Video segments templates](/images/docs/cdn/getting-started/create-a-cdn-resource/create-a-cdn-resource-for-video-streaming/create-a-cdn-resource-for-video-streaming-image2.png)</Frame>

        3. Review the pre-filled **Cache time** (2 seconds) and **Browser cache** (0 seconds) values, and adjust if needed.

        4. Click **Create rule**.

        For MPEG-DASH manifests (`.mpd`) or a custom pattern, click **Create blank rule** instead and enter the pattern manually:

        - Rule name: `manifests`
        - Pattern: `^/.+\.(m3u8|mpd|ism)$` (regex for manifest files)
        - Cache time: 2 seconds
        - Browser cache: 0 seconds (no browser caching for manifests)

        These values balance freshness with cache efficiency: segments at 60 seconds stay longer than typical segment duration for slow connections, while manifests at 2 seconds keep the segment list current. If DVR is enabled, the segment cache should match the DVR window.

        For LL-HLS (Low-Latency HLS):

        1. Navigate to **CDN Resource** > **Cache**.

        2. Enable **Query parameters allowlist**.

        3. Add these parameters: `_HLS_msn`, `_HLS_part`, `_HLS_skip`.
           - These are required for LL-HLS to work correctly
           - All other query parameters will be ignored
      </Tab>
      <Tab title="VOD">
        Optimized cache configuration for VOD.

        Enable Always online first:

        1. Navigate to **CDN Resource** > **Cache**.

        2. Enable **Always online** and add these tags to the list of error types that trigger it:
           - error (serve stale cache if origin returns errors)
           - timeout (serve stale cache if origin times out)

        Always online keeps cached videos available if the origin goes down, so "origin unavailable" errors do not reach viewers. This matters most for high-traffic VOD workloads.

        <Warning>
        Do not add "updating" to the error types list for VOD. When excluded, the CDN can deliver video chunks more efficiently and reduce buffering. Only add "error" and "timeout".
        </Warning>

        Set long-term cache for all content:

        1. Still in **CDN Resource** > **Cache**.

        2. Set **CDN cache expiry time** to 1 year (31,536,000 seconds).
           - This caches all successful responses (2xx, 3xx) for 1 year
           - VOD content does not change, so aggressive caching is safe

        3. Set **Custom cache time for 404 errors** to 1 second.
           - Protects the origin from repeated requests for non-existent files

        A one-year TTL is safe because VOD files do not change once uploaded, so maximum cache efficiency keeps origin load low. Purge manually when a file must be replaced.

        (Optional) Large file optimization for MP4 progressive download:

        1. Navigate to **CDN Resource** > **Content**.

        2. Enable **Large files delivery optimization**.
           - Enables HTTP range requests for seeking
           - The origin must support Range requests
           - Enabling this invalidates existing cache

        Requirements:

        - Origin server supports HTTP Range Request (Content-Range headers)
        - If using an origin group, files must have identical Content-Length and ETag across all origins
        - Do not enable GZIP compression when using large file optimization

        (Optional) Query string handling:

        1. Navigate to **CDN Resource** > **Cache**.

        2. Enable **Ignore query string** if VOD URLs do not use query parameters for logic, and caching `video.mp4?ref=email` and `video.mp4?ref=social` as the same file is preferred.

        3. Confirm whether analytics or tracking depends on query parameters before enabling this option.

        Common file extensions:

        - Manifests: `.m3u8`, `.mpd`
        - Segments: `.ts`, `.m4s`, `.m4v`, `.m4a`, `.m4i`
        - Progressive: `.mp4`, `.webm`
        - Subtitles: `.vtt`, `.webvtt`, `.ttml`
      </Tab>
      <Tab title="Live MPEG-DASH CMAF">
        <Warning>
        When using MPEG-DASH with an external transcoder (not Gcore Video Streaming), enable the Chunked-Proxy module for low-latency delivery by contacting Gcore Support or the account manager.

        Without Chunked-Proxy, the CDN uses standard store-and-forward delivery, resulting in 10–15 second latency instead of the 2–4 second low-latency performance.
        </Warning>

        ### Chunked-Proxy module

        Standard CDNs wait for files to download fully from origin before sending them to viewers (store and forward). The Chunked-Proxy module streams data immediately: the CDN edge sends bytes to viewers while receiving from origin, using Chunked Transfer Encoding (HTTP/1.1) and Framing (HTTP/2/3). Chunked-Proxy delivers the 2–4 second latency described in [low-latency&nbsp;streaming](/streaming/live-streaming/how-low-latency-streaming-works).

        ### Chunked-Proxy activation

        Request activation from Support:

        1. Contact Gcore Support or the account manager.

        2. Request Chunked-Proxy module activation for the CDN resource.

        3. Provide:
           - The CDN resource ID or domain
           - Confirmation of using LL-DASH with CMAF segments
           - Origin transcoder details (Wowza or Nginx-RTMP)

        After activation, LL-DASH streams achieve 2–4 second latency, and the CDN streams chunks immediately as they arrive from origin.

        <Info>
        When using Gcore Video Streaming (the Gcore transcoding service), Chunked-Proxy is already enabled by default. Activation is only needed for an external transcoder.
        </Info>

        ### Cache configuration

        After Support activates Chunked-Proxy, follow the cache configuration from the Live HLS tab.

        The only difference is the manifest caching rule pattern: use `^/.+\.mpd$` for DASH manifest files instead of `.m3u8`.

        Common DASH/CMAF file extensions:

        - Manifests: `.mpd`
        - Segments: `.m4s`, `.m4v`, `.m4a`, `.cmfv`, `.cmfa`
        - Initialization: `.mp4` (init segments)
      </Tab>
      <Tab title="FLV">
        FLV streams use a single continuous connection, so cache settings are custom and require Support configuration. The [FLV&nbsp;streaming](/cdn/video-cdn/flv-streaming) guide lists the recommended values for this format.
      </Tab>
    </Tabs>

    <Info>
    For high-traffic streaming or specific performance requirements, contact Gcore Support to configure cache sharding across multiple servers, cache lock timing for concurrent requests, custom TTL strategies per file type, and origin connection limits that protect the origin from traffic spikes.
    </Info>
  </Step>

  <Step title="Enable the LIVE STREAMING preset">
    <Info>
    For live streams only — skip this step when delivering VOD.
    </Info>

    The LIVE STREAMING preset enables RAM-based caching for reduced latency.

    1. Navigate to CDN resources.

    2. Click the **three dots** next to the Video CDN resource.

    3. Select **Turn on LIVE STREAMING preset**.

    <Frame>![Turn on LIVE STREAMING preset](/images/docs/cdn/getting-started/create-a-cdn-resource/create-a-cdn-resource-for-video-streaming/create-a-cdn-resource-for-video-streaming-image3.png)</Frame>

    It caches video segments in RAM instead of disk for faster delivery and lower latency.
  </Step>
</Steps>

---

## Test the stream

Confirm that the CDN URL plays correctly before going to production.

### CDN URL

Update stream URLs from origin to CDN.

Before (origin):

```text
http://live.example.com/playlist.m3u8
```

After (CDN):

```text
http://video.example.com/playlist.m3u8
```

Or with an auto-generated domain:

```text
http://123456.gcdn.co/playlist.m3u8
```

### Test playback

Confirm playback in a player that supports HLS:

- Browser: [VLC&nbsp;Media&nbsp;Player](https://videolan.org)
- Online: [HLS.js&nbsp;demo&nbsp;player](https://hls-js.netlify.app/demo/)
- Mobile: Native Safari (iOS) or Chrome (Android)

Pasting the CDN URL into the player verifies playback.

---

## Advanced configuration

After the resource is live, Origin Shielding, geo-restrictions, and secure tokens can further protect the origin and control who can watch.

### Origin Shielding

For high-traffic streams (100+ concurrent viewers), enable Origin Shielding to cut origin load.

<Steps>
  <Step title="Open Origin Shielding settings">
    Navigate to **CDN Resource** > **General** > **Origin shielding**.
  </Step>
  <Step title="Enable and choose a location">
    Toggle **Enable origin shielding**, then select the shield location closest to the origin.
  </Step>
</Steps>

Origin Shielding reduces origin requests by 80–95%, which protects the origin from traffic spikes. Additional options, including shield location selection, are available in [Origin&nbsp;Shielding](/cdn/cdn-resource-options/general/enable-and-configure-origin-shielding) settings.

### Geo-restrictions

Restrict content by country when broadcasting rights require it.

<Steps>
  <Step title="Open the country access policy">
    Navigate to **CDN Resource** > **Security** > **Country access policy**.
  </Step>
  <Step title="Choose allow or block">
    Select **Allow only** to make content available only in selected countries, or **Block** to block content in selected countries.
  </Step>
  <Step title="Apply the countries">
    Select countries, then save changes.
  </Step>
</Steps>

Example: "Allow only in Dominican Republic" for regional sports rights.

The [Geo-restrictions](/cdn/cdn-resource-options/security/control-access-to-the-content-with-country-referrer-ip-and-user-agents-policies#country-access-policy) policy also supports combinations with referrer, IP, and user-agent rules.

### Secure tokens

Protect content with time-limited authenticated URLs for paid content, private videos, and time-limited previews.

<Steps>
  <Step title="Open Secure Token settings">
    Navigate to **CDN Resource** > **Security** > **Secure Token**.
  </Step>
  <Step title="Configure and generate tokens">
    Enable and configure token settings, then generate tokens on the backend for each user.
  </Step>
</Steps>

[Secure&nbsp;Tokens](/cdn/cdn-resource-options/security/use-a-secure-token/about-secure-token) control generation and expiration for each signed URL.

---

## Troubleshooting

Use these checks when a stream fails to play or latency is higher than expected. Contact Gcore Support or use the chat in the bottom-right corner if the issue remains.

### Stream not playing

1. Check that the origin is accessible: test the origin URL directly in a browser.

2. Verify DNS: confirm the CNAME points to the CDN (for a custom domain).

3. Check cache rules: verify `.m3u8` and `.ts` rules are configured.

4. Review CDN logs: check for 4xx/5xx errors in [CDN&nbsp;statistics](/cdn/view-statistics-of-a-cdn-resource).

### High latency

1. Enable the LIVE STREAMING preset for live streams (see the last step in Quick setup).

2. Reduce playlist cache: set `.m3u8` cache to 1–2 seconds.

3. Enable Origin Shielding to reduce routing hops.

4. Contact Support for advanced latency optimization.
