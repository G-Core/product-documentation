---
title: Use the X-Forwarded-Proto header to identify the protocol used for CDN servers requests
sidebarTitle: Identify a user's request protocol
ai-navigation: Use the X-Forwarded-Proto header that Gcore CDN edge servers send by default to detect HTTP or HTTPS client requests on the origin.
---

The `X-Forwarded-Proto` header identifies the protocol (HTTP or HTTPS) that a client used to connect to a CDN edge server.

Gcore CDN edge servers send `X-Forwarded-Proto` on origin requests by default. The [Gcore Customer Portal](https://portal.gcore.com) has no toggle for this header — edge servers always include it. Typical values are `http` and `https`.

Origin or application logic can read the header to:

- Redirect HTTP clients to HTTPS
- Serve different content based on the request protocol
