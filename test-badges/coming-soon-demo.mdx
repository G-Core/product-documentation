---
title: "Create an AI-accelerated load balancer"
description: "Creates a load balancer with AI-assisted traffic steering. This endpoint is not available yet."
tag: "Coming soon"
---

<Info>
This endpoint is not available yet. <Badge color="yellow">Coming soon</Badge>
</Info>

`POST /cloud/v1/lbaas/{project_id}/{region_id}/loadbalancers/ai-accelerator` <Badge color="yellow">Coming soon</Badge>

---

## Path parameters

<ParamField path="project_id" type="integer" required>
  Numeric project identifier.
</ParamField>

<ParamField path="region_id" type="integer" required>
  Numeric region identifier.
</ParamField>

---

## Body parameters

<ParamField body="name" type="string" required>
  Display name for the load balancer.
</ParamField>

<ParamField body="steering_mode" type="string">
  Traffic steering strategy. One of: `latency`, `cost`, `availability`. <Badge color="yellow">Coming soon</Badge>
</ParamField>

---

## Badge variants

The same `<Badge>` component accepts any label and color. These are the variants that map to typical API statuses:

<Badge color="blue">BETA</Badge>
<Badge color="yellow">Coming soon</Badge>
<Badge color="green">NEW</Badge>
<Badge color="orange">Preview</Badge>
<Badge color="gray">lb2 only</Badge>
<Badge color="red">Deprecated</Badge>
<Badge color="purple">Enterprise</Badge>
