---
title: Check storages usage reports
sidebarTitle: View statistics
ai-navigation: View storage usage reports in Statistics section with metrics for used space, requests, traffic, and number of objects; filter by time period (day, week, month, year, custom), storage type (Object Storage, SFTP Storage), location, and name; data aggregates hourly max and average values.
---

Storage usage metrics are available in the **Statistics** section of the [Gcore Customer Portal](https://portal.gcore.com). Gcore uses the following metrics for storage billing:

- Used space
- Requests
- Traffic
- Number of objects

The page supports filtering by time period (last 24 hours, last 48 hours, last week, last month, last year, or a custom date range), storage type (S3 or SFTP), location, and storage name.

<Frame>
  ![Statistics page showing metric tabs and filter options for storage type, location, and time period](/images/docs/storage/check-storages-usage-reports/Screenshot_70.png)
</Frame>

Statistics are not available immediately — data appears in the portal within a couple of hours after storage creation. The graphs display aggregated values: used space is measured every hour and shown as max and average usage for the selected period. Requests for longer periods — a year of data — may take more time to process.

Used space is rounded to the nearest whole GB for billing. For SFTP storage, system files — SSH keys, empty directories, and a `robots.txt` file — account for about 350 kB of used space. If a storage was created but no files have been uploaded, statistics will reflect only this system-file space.
