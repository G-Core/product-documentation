# TEST - custom llms.txt from repo

This file was placed manually in the repository root to test
whether Mintlify serves it or overrides it with its own generated version.

If you see this text, the repo file takes priority over Mintlify generation.
