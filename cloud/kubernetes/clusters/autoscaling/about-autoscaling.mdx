---
title: About autoscaling
sidebarTitle: Overview
ai-navigation: Configure Kubernetes node pool autoscaling with minimum and maximum node limits; autoscaling deploys nodes when pods exceed available resources and removes idle nodes after 20 minutes of inactivity.
---

## Autoscaling overview

Autoscaling adjusts the number of nodes in a pool based on changes in load. If the existing nodes cannot handle the load, a new one is deployed. If some nodes are idle without any load, they are removed.

## Autoscaling behavior

The minimum and maximum number of nodes are configured, and autoscaling keeps the node count within those limits. More nodes than the specified maximum will not be created, and nodes will not be removed if the minimum has been reached.

- If a new pod (application container group) needs to run and the existing nodes do not have enough resources, autoscaling deploys a new node. Available resources are determined by `resources.requests`.
- If some nodes run without any pods for 20 minutes, autoscaling removes them.
- If nodes with running pods are manually deleted, reducing the count below the autoscaling minimum, the system reassesses the pool load after 20 minutes. If the remaining nodes cannot handle the required pods, more nodes are added; otherwise, the count stays as-is.

## Change autoscaling settings

To update the minimum and maximum node count for an existing pool, follow these steps.

1\. In the Gcore Customer Portal, navigate to **Cloud** > **Managed Kubernetes**, then click the cluster name.

<Frame>![Screenshot_2023-02-27_at_13.37_2.png](/images/docs/cloud/kubernetes/clusters/autoscaling/about-autoscaling/13323994996881.png)</Frame>

2\. Open the **Pools** section, click **⋯** next to the pool, and select **Change autoscaling limits**.

<Frame>![Screenshot_2023-02-27_at_20.42_1.jpg](/images/docs/cloud/kubernetes/clusters/autoscaling/about-autoscaling/13328028097425.png)</Frame>

3\. Enter the maximum and minimum number of nodes and click **Save**.

The new autoscaling settings are applied.

## Disable autoscaling

When autoscaling is disabled, the node count in the pool stays constant.

1\. In the Gcore Customer Portal, navigate to **Cloud** > **Managed Kubernetes**, then click the cluster name.

<Frame>![Screenshot_2023-02-27_at_13.37_2.png](/images/docs/cloud/kubernetes/clusters/autoscaling/about-autoscaling/13325419009809.png)</Frame>

2\. Open the **Pools** section, click **⋯** next to the pool, and select **Change autoscaling limits**.

<Frame>![Screenshot_2023-02-27_at_20.42_1.jpg](/images/docs/cloud/kubernetes/clusters/autoscaling/about-autoscaling/13328028097425.png)</Frame>

3\. Set the minimum and maximum node counts to the same value and click **Save**.

Autoscaling is now disabled. The pool maintains the specified node count regardless of load.
