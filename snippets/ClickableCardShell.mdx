{/*
  Full-card click target without wrapping inner <a> bullets in an outer <a> (invalid HTML).
  Overlay link sits under the card; bullet links use pointer-events: auto (see custom.css).
*/}

export const ClickableCardShell = ({ href, label, children }) => (
  <div className="homepage_clickable_card_wrap relative h-full">
    <a href={href} className="homepage_card_stretched_link" aria-label={label} />
    <div className="homepage_clickable_card_foreground relative z-[1] h-full">
      {children}
    </div>
  </div>
);
